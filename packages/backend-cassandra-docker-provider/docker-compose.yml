version: '3.9'

services:
  db:
    build:
      context: .
      dockerfile: Dockerfile
    networks:
      - cassandra_network
    ports:
      - 9042:9042

networks:
  cassandra_network:
    driver: bridge
